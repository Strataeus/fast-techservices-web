# Deprecated Components (À ne pas utiliser)

Les composants suivants sont dupliqués ou obsolètes. Ils sont maintenus en archive mais **ne doivent pas être importés** dans le code actif.

## Composants à supprimer lors de la prochaine refonte

### 1. `components/Section.tsx` ❌
**Raison** : Doublon de `components/ui/Section.tsx`  
**Remplacé par** : `components/ui/Section`  
**Utilisage actuel** : AUCUN (déjà corrigé en not-found.tsx)

### 2. `components/Header.tsx` ❌
**Raison** : Obsolète, remplacé par `components/SiteHeader.tsx`  
**Remplacé par** : `components/SiteHeader` (importé dans app/layout.tsx)  
**Utilisage actuel** : AUCUN

### 3. `components/Footer.tsx` ❌
**Raison** : Obsolète, remplacé par `components/SiteFooter.tsx`  
**Remplacé par** : `components/SiteFooter` (importé dans app/layout.tsx)  
**Utilisage actuel** : AUCUN

### 4. `components/Method.tsx` ⚠️
**Raison** : À vérifier si utilisé (proche de MethodFAST.tsx)  
**Remplacé par** : `components/MethodFAST.tsx` (recommandé)  
**Utilisage actuel** : À scanner

---

## Composants à garder (Actifs)

✅ `components/SiteHeader.tsx` - Navigation principale (utilisé dans layout.tsx)  
✅ `components/SiteFooter.tsx` - Footer principal (utilisé dans layout.tsx)  
✅ `components/ui/Section.tsx` - Composant réutilisable de section  
✅ `components/MethodFAST.tsx` - Composant méthode FAST

---

## Prochaines étapes

- [ ] Supprimer les fichiers dépréciés lors de la prochaine version majeure
- [ ] Documenter les imports corrects dans le README
- [ ] Ajouter une règle ESLint pour éviter les imports dépréciés (optional)
